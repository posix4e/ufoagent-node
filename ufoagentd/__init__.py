"""UFOAgent node daemon.

Links a Windows machine running Microsoft UFO2 to the UFOAgent control plane and keeps a
short-lived LLM credential fresh in UFO2's ``config/ufo/agents.yaml``. Pure standard library.
"""

__version__ = "0.1.0"
